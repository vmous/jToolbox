/**
 * <em>GoF</em> - Attach additional responsibilities to an object dynamically
 * keeping the same interface. Decorators provide a flexible alternative to
 * sub-classing for extending functionality.
 */
package toolbox.design.patterns.structural.decorator;