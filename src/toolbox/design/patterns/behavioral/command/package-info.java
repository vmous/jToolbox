/**
 * <em>GoF</em> - Encapsulate a request as an object, thereby letting you
 * parameterize clients with different requests, queue or log requests,
 * and support undo-able operations.
 */
package toolbox.design.patterns.behavioral.command;