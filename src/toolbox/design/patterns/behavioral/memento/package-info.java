/**
 * <em>GoF</em> - Without violating encapsulation, capture and externalize an
 * object's internal state allowing the object to be restored to this state
 * later.
 */
package toolbox.design.patterns.behavioral.memento;