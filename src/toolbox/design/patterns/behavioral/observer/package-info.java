/**
 * <em>GoF</em> - Define a one-to-many dependency between objects where a
 * state change in one object results with all its dependents being notified
 * and updated automatically.
 */
package toolbox.design.patterns.behavioral.observer;